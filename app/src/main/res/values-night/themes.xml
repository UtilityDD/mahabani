<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme for Dark Mode. -->
    <style name="Base.Theme.KadaChabuk" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/dark_primary</item>
        <item name="colorOnPrimary">@color/dark_onPrimary</item>
        <item name="colorPrimaryContainer">@color/dark_primaryContainer</item>
        <item name="colorOnPrimaryContainer">@color/dark_onPrimaryContainer</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/dark_secondary</item>
        <item name="colorOnSecondary">@color/dark_onSecondary</item>
        <item name="colorSecondaryContainer">@color/dark_secondaryContainer</item>
        <item name="colorOnSecondaryContainer">@color/dark_onSecondaryContainer</item>
        <!-- Surface colors for backgrounds, cards, etc. -->
        <item name="android:colorBackground">@color/dark_surface</item>
        <item name="colorSurface">@color/dark_surface</item>
        <item name="colorOnSurface">@color/dark_onSurface</item>
        <item name="colorSurfaceContainer">@color/dark_surfaceContainer</item>
        <item name="colorOnSurfaceVariant">@color/dark_onSurfaceVariant</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorSurfaceContainerHighest</item>

        <item name="searchViewStyle">@style/App.Widget.SearchView</item>

        <!-- Set the default font for the app -->
        <item name="android:fontFamily">@font/lato</item>
    </style>

    <style name="Theme.KadaChabuk" parent="Base.Theme.KadaChabuk" />

    <style name="App.Widget.SearchView" parent="Widget.AppCompat.SearchView">
        <item name="android:textColor">?attr/colorOnSurface</item>
        <item name="android:textColorHint">?attr/colorOnSurfaceVariant</item>
    </style>

</resources>